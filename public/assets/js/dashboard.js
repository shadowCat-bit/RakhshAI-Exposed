// new SimpleBar(document.getElementById('chats-scroll'), { autoHide: false });
// new SimpleBar(document.getElementById('chat-history'), { autoHide: true });

// const textarea = document.getElementById("message-input");

// textarea.addEventListener("input", () => {
//   textarea.style.height = "auto"; // Reset the textarea height to auto

//   // Set the new height based on the scroll height of the textarea
//   textarea.style.height = `${textarea.scrollHeight}px`;
// });